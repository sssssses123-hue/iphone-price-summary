# iPhone 價格網站編輯與維護說明

## 一、平常要改哪裡？

全站價格、年度說明、重要節點和選購建議都集中在 `data.js`。

### 修改現有資料

找到對應年份與機型：

```js
{ name: "iPhone 18 Pro", usd: 1199, cny: 9999, gb: 256 }
```

- `usd`：美國首發起售價（美元）
- `cny`：中國大陸首發起售價（人民幣）
- `gb`：起始容量

修改後存檔，用瀏覽器重新整理 `index.html` 就能看到結果。

### 新增一年

在 `window.IPHONE_DATA` 陣列中新增：

```js
{
  year: 2027,
  models: [
    { name: "iPhone 19", usd: 899, cny: 6799, gb: 256 }
  ],
  cheapestUsd: 899,
  note: "這一年產品線的重點。"
}
```

`cheapestUsd` 要填該年最便宜新款 iPhone 的美元起售價，走勢圖會使用這個數字。

## 二、自動監測如何運作？

GitHub Actions 工作流程：

```text
.github/workflows/apple-price-watch.yml
```

它每天 UTC 02:23（北京時間 10:23）執行一次，也可以到 GitHub 倉庫的 **Actions → Apple iPhone Price Watch → Run workflow** 手動啟動。

監測程式：

```text
scripts/check-apple-prices.mjs
```

會檢查 Apple 官方美國與中國大陸 iPhone 購買頁，並將結果和下列基準比較：

```text
monitor/apple-price-state.json
```

## 三、自動監測會做什麼？

### 1. 發現全新機型

如果 Apple 官方美、中頁面都能確認價格與起始容量，工作流程會：

- 把新機型加入 `data.js` 當年度資料
- 更新 `SITE.updated`
- 自動 commit 並 push
- 由 GitHub Pages 發布新網站版本

自動加入的年度說明會先使用基本文字，之後可自行在 `data.js` 補充產品亮點。

### 2. 發現既有機型價格、容量或陣容變動

為避免把後來售價或促銷價誤寫成首發價，工作流程不會改寫既有歷史資料，而是：

- 更新 `monitor/apple-price-state.json`
- 產生 `monitor/latest-report.md`
- 開啟一則 GitHub Issue 通知

開啟 Issue 後，你可以人工判斷這是不是新年度產品線變化，再決定是否更新 `data.js`。

### 3. 沒有變動

不會每天 commit、不會開 Issue，也不會產生無意義的檔案變更。為避免 GitHub 在倉庫超過 60 天沒有活動後停用排程，`monitor/heartbeat.json` 會每 30 天更新一次；心跳更新只會 commit 檔案，不會開啟通知 Issue。

## 四、在哪裡看每次更新通知？

到 GitHub 倉庫的 **Issues** 頁面，查看標題類似：

```text
[Apple Price Watch] 2026-09-20 Apple 官方價格／陣容更新
```

Issue 內容會列出：

- 自動寫入的機型
- 官方頁面價格或容量變動
- 目前 Apple 美國與中國大陸頁面的起售資訊

GitHub 若已開啟郵件通知，也會同步收到 Issue 通知。

## 五、本機測試

沒有網路時，可以先保存 Apple HTML，再執行：

```powershell
node scripts/check-apple-prices.mjs --samples-dir "C:\你的\HTML資料夾" --dry-run
```

只檢查、不寫入檔案時加上 `--dry-run`。正式測試會寫入 `monitor/apple-price-state.json`，第一次執行只建立基準，不會產生更新通知。

## 六、注意事項

- 請維持 `data.js` 為 UTF-8 編碼。
- 歷史首發價不要直接改成 Apple 現在的零售價。
- 若 Apple 大幅改版 HTML，監測程式可能找不到頁面資料；此時 GitHub Actions 會失敗，可在 Actions 記錄中查看原因。
- 若想調整檢查時間，修改 `.github/workflows/apple-price-watch.yml` 的 cron 表達式。GitHub Actions 使用 UTC 時間。